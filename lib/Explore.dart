import "package:flutter/material.dart";

class ExplorePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    
    return new Container(
      child: new Center(
        child: new Icon(Icons.local_airport, size: 80, color: Colors.indigo),
      ),
    );
  }
}